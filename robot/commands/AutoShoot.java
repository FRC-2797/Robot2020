package frc.robot.commands;

import edu.wpi.first.wpilibj2.command.CommandBase;
import frc.robot.subsystems.Drivetrain;
import frc.robot.subsystems.Shooter;

public class AutoShoot extends CommandBase{
    /*

    public void leftShoot(DriveDistance drive,  Drivetrain drivetrain, TurnToAngleProfiled profiledAngle, TurnToAngleLimelight limelight, Shooter shoot){
        drivetrain.resetSensor();
        drive.driveDistanceFWD(136, drivetrain);
        drivetrain.resetSensor();
        drive.driveDistanceSide(66.44, drivetrain);
        drivetrain.resetSensor();
        profiledAngle = new TurnToAngleProfiled(180, drivetrain);
        limelight.drive?()
        drive.driveDistanceFWD(90, drivetrain);
        //limelight = new TurnToAngleLimelight(drivetrain);
        //shoot.shoot();
    }

    public void rightShoot(DriveDistance drive,  Drivetrain drivetrain, TurnToAngleProfiled profiledAngle, TurnToAngleLimelight limelight, Shooter shoot){
        drivetrain.resetSensor();
        drive.driveDistanceFWD(136, drivetrain);
        drivetrain.resetSensor();
        drive.driveStrafe(179.32);
        profiledAngle = new TurnToAngleProfiled(0, drivetrain);
        drivetrain.resetSensor();

    }

    public void middleShoot(DriveDistance drive,  Drivetrain drivetrain, TurnToAngleProfiled profiledAngle, TurnToAngleLimelight limelight, Shooter shoot){
        drivetrain.resetSensor();
        drive.driveDistanceFWD(150,drivetrain);
        drivetrain.resetSensor();
        drive.driveDistanceSide(66.44);
        drivetrain.resetSensor();
        limelight.go?
    }
    */
}