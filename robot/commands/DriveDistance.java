package frc.robot.commands;

import frc.robot.subsystems.Drivetrain;

public class DriveDistance{
    /*
    
    //navx distance not encoder 
    //private double wheelCircumference = 18.85;
    double initPosition;
    public void driveDistanceFWD(double goalPosition, Drivetrain drivetrain){
        while(drivetrain.getEncoder() <= goalPosition){
            drivetrain.drive(0.0,.2,0.0,false);
        } 
    }

    public void driveDistanceSide(double goalPosition, Drivetrain drivetrain, boolean side){
        if(side){
            
        }
        
    }
    */
    
}